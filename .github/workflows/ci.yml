name: CI - Frontend
on: [push]
jobs:
  docker-build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v6
      - name: build image
        run: make docker-build

  
        




        
